import Link from 'next/link'; import {Card} from '@/components/ui'; export default function Tests(){return <div className="p-5 md:p-8 max-w-6xl mx-auto"><h1 className="text-3xl font-black">Тесты</h1><p className="text-gray-500 mt-1">Проверь знания и найди пробелы.</p><div className="grid md:grid-cols-3 gap-4 mt-7">{['Математика — алгебра','Английский — времена','Физика — механика'].map((t,i)=><Card className="p-5" key={t}><span className="text-sm text-gray-500">Тест {i+1}</span><h2 className="font-bold text-lg mt-2">{t}</h2><p className="text-sm text-gray-500 mt-2">15 вопросов • 20 минут</p><Link href={`/tests/${i+1}`} className="inline-block mt-5 text-[#635bff] font-bold">Пройти →</Link></Card>)}</div></div>}