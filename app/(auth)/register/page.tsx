'use client';
import Link from 'next/link'; import {FormEvent,useState} from 'react'; import {createClient} from '@/lib/supabase/client'; import {Button} from '@/components/ui';
export default function Register(){const[name,setName]=useState('');const[email,setEmail]=useState('');const[p,setP]=useState('');const[error,setError]=useState('');const[done,setDone]=useState(false);const[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');const{data,error}=await createClient().auth.signUp({email,password:p,options:{data:{name}}});if(error)setError(error.message);else if(data.session)location.href='/onboarding';else setDone(true);setLoading(false);}return <div className="min-h-screen grid place-items-center p-5"><div className="w-full max-w-md"><Link href="/" className="text-2xl font-black"><span className="text-[#635bff]">Edu</span>AI</Link><div className="card p-7 mt-6"><h1 className="text-2xl font-bold">Создай аккаунт 🚀</h1>{done?<p className="mt-5 text-gray-600">Проверь почту и перейди по ссылке подтверждения, чтобы войти в EduAI.</p>:<form className="space-y-4 mt-6" onSubmit={submit}><input required value={name} onChange={e=>setName(e.target.value)} placeholder="Имя" className="w-full border rounded-xl p-3"/><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" className="w-full border rounded-xl p-3"/><input required minLength={6} type="password" value={p} onChange={e=>setP(e.target.value)} placeholder="Пароль (минимум 6 символов)" className="w-full border rounded-xl p-3"/>{error&&<p className="text-sm text-red-600">{error}</p>}<Button disabled={loading} className="w-full">{loading?'Создаём…':'Создать аккаунт'}</Button></form>}<p className="text-center mt-5 text-sm">Уже есть аккаунт? <Link href="/login" className="text-[#635bff]">Войти</Link></p></div></div></div>}
